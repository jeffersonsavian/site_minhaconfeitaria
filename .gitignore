# build
# dist/ é versionado de propósito: o ServerAvatar serve dist/ direto (sem buildar no servidor).
# O hook pre-push reconstrói dist/ automaticamente antes de cada push.
.astro/

# deps
node_modules/

# logs
npm-debug.log*
*.log

# env / sistema
.env
.env.*
.DS_Store
Thumbs.db
